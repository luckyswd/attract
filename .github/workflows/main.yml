name: 🚀 Deploy website
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 📩 Get latest code
        uses: actions/checkout@v3
#
#      - name: Run script file
#        run: |
#          chmod +x ./deploy.sh
#          ./deploy.sh
#        shell: bash

      - name: 📦 Get LFTP
        run: sudo apt install lftp
      - name: 🛠️ Configure LFTP
        run: mkdir ~/.lftp && echo "set ssl:verify-certificate false;" >> ~/.lftp/rc
      - name: 🔑 Load Secrets
        run: echo "machine luckyswd@attract.company login luckyswd@attract.company password NNCNUQU&xn3xm0kZiN&aQG5P" > ~/.netrc
      - name: test folder
        run: cd wp-app && pwd
      - name: 📁 Upload Folder
        run: lftp -e "mirror --parallel=100 -R ./wp-app/wp-content/themes/attract/dist/ /public/" luckyswd@attract.company

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: serwer2390849.home.pl
          username: luckyswd@attract.company
          password: ${{ secrets.ftp_password }}
          local-dir: ./wp-app/
          server-dir: "./public/"
